import React from 'react';
import { PageHeader } from 'react-bootstrap';

const OrderComplete = () => {
  return (
    <PageHeader>Your Order has been completed</PageHeader>
  );
};

export default OrderComplete;
